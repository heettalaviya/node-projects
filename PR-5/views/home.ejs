<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Cinematic Movie Hub</title>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Poppins", sans-serif;
        }

        body {
            background: radial-gradient(circle at top, #1b1f2a, #0b0d13);
            color: #ffffff;
            min-height: 100vh;
        }

        /* HEADER TITLE */
        .cinema-title {
            text-align: center;
            font-size: 32px;
            letter-spacing: 2px;
            margin: 40px 0 10px;
        }

        .cinema-subtitle {
            text-align: center;
            color: #9ca3af;
            margin-bottom: 40px;
        }

        /* GRID */
        .movie-container {
            padding: 0 60px 60px;
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
            gap: 35px;
        }

        /* CARD */
        .movie-card {
            background: #0f1320;
            border-radius: 18px;
            overflow: hidden;
            position: relative;
            box-shadow: 0 30px 60px rgba(0,0,0,0.7);
            transition: 0.5s ease;
        }

        .movie-card::after {
            content: "";
            position: absolute;
            inset: 0;
            background: linear-gradient(180deg, transparent 40%, rgba(0,0,0,0.9));
            opacity: 0;
            transition: 0.4s;
        }

        .movie-card:hover::after {
            opacity: 1;
        }

        .movie-card:hover {
            transform: scale(1.06);
        }

        /* IMAGE */
        .movie-img {
            height: 380px;
        }

        .movie-img img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            filter: brightness(0.95);
        }

        /* RATING */
        .rating {
            position: absolute;
            top: 14px;
            left: 14px;
            background: rgba(255,193,7,0.95);
            color: #000;
            font-size: 13px;
            padding: 6px 12px;
            border-radius: 30px;
            font-weight: 600;
            z-index: 2;
        }

        /* INFO */
        .movie-info {
            position: absolute;
            bottom: 0;
            padding: 20px;
            z-index: 2;
            width: 100%;
        }

        .movie-info h3 {
            font-size: 20px;
            margin-bottom: 6px;
        }

        .movie-info p {
            font-size: 14px;
            color: #d1d5db;
        }

        .tag {
            display: inline-block;
            margin-top: 8px;
            background: rgba(255,255,255,0.1);
            padding: 4px 10px;
            font-size: 12px;
            border-radius: 20px;
        }

        /* ACTIONS */
        .actions {
            margin-top: 12px;
            display: flex;
            gap: 10px;
        }

        .actions a {
            text-decoration: none;
            padding: 8px 14px;
            font-size: 13px;
            border-radius: 30px;
            transition: 0.3s;
        }

        .edit {
            background: rgba(59,130,246,0.2);
            color: #60a5fa;
        }

        .edit:hover {
            background: #3b82f6;
            color: #ffffff;
        }

        .delete {
            background: rgba(239,68,68,0.2);
            color: #f87171;
        }

        .delete:hover {
            background: #ef4444;
            color: #ffffff;
        }

        /* EMPTY */
        .no-movie {
            text-align: center;
            color: #9ca3af;
            font-size: 18px;
            margin-top: 80px;
        }

        /* RESPONSIVE */
        @media(max-width: 600px) {
            .movie-container {
                padding: 0 20px 40px;
            }

            .movie-img {
                height: 320px;
            }
        }
    </style>
</head>

<body>

    <%- include('header.ejs') %>

    <h1 class="cinema-title">CINEMATIC MOVIE HUB</h1>
    <p class="cinema-subtitle">Experience movies like a theatre 🎥</p>

    <% if (movies.length === 0) { %>
        <p class="no-movie">No movies available 🍿</p>
    <% } %>

    <div class="movie-container">
        <% movies.forEach(movie => { %>

            <div class="movie-card">
                <span class="rating">⭐ <%= movie.rating || "N/A" %></span>

                <div class="movie-img">
                    <img src="<%= movie.ProfileImage %>" alt="movie poster">
                </div>

                <div class="movie-info">
                    <h3><%= movie.title %></h3>
                    <p>🎞 <%= movie.year %></p>
                    <span class="tag"><%= movie.genre %></span>

                    <div class="actions">
                        <a href="/edit-movie/<%= movie._id %>" class="edit">Edit</a>
                        <a href="/delete-movie/<%= movie._id %>" class="delete">Delete</a>
                    </div>
                </div>
            </div>

        <% }) %>
    </div>

    <%- include('footer.ejs') %>

</body>
</html>
