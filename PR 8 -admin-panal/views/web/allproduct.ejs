<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Heet Store | Premium Collection</title>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;600;800&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        :root {
            --primary-dark: #1a1a1a;
            --accent-color: #c5a059; /* Luxury Gold */
            --bg-light: #fcfcfc;
            --card-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
            --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        body {
            margin: 0;
            font-family: 'Plus Jakarta Sans', sans-serif;
            background: var(--bg-light);
            color: var(--primary-dark);
        }

        /* ===== MODERN NAVBAR ===== */
        header {
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(10px);
            padding: 15px 5%;
            border-bottom: 1px solid #eee;
            position: sticky;
            top: 0;
            z-index: 1000;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .logo {
            font-size: 22px;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 2px;
            color: var(--primary-dark);
        }

        .search-container {
            flex: 0 1 400px;
            position: relative;
        }

        .search-container input {
            width: 100%;
            padding: 10px 20px;
            padding-left: 45px;
            border-radius: 50px;
            border: 1px solid #e0e0e0;
            background: #f5f5f5;
            transition: var(--transition);
        }

        .search-container input:focus {
            background: #fff;
            border-color: var(--accent-color);
            outline: none;
            box-shadow: 0 0 0 4px rgba(197, 160, 89, 0.1);
        }

        /* ===== CATEGORY NAV ===== */
        .nav-categories {
            display: flex;
            justify-content: center;
            gap: 30px;
            padding: 15px;
            background: #fff;
            border-bottom: 1px solid #f0f0f0;
        }

        .category-item {
            position: relative;
            font-weight: 600;
            font-size: 14px;
            cursor: pointer;
            color: #666;
            transition: var(--transition);
        }

        .category-item:hover {
            color: var(--accent-color);
        }

        /* ===== PRODUCT GRID ===== */
        .section-title {
            padding: 40px 5% 20px;
            font-weight: 800;
            font-size: 2rem;
        }

        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 30px;
            padding: 0 5% 50px;
        }

        .product-card {
            background: #fff;
            border-radius: 20px;
            overflow: hidden;
            border: none;
            transition: var(--transition);
            position: relative;
        }

        .product-card:hover {
            transform: translateY(-10px);
            box-shadow: var(--card-shadow);
        }

        .image-wrapper {
            background: #f9f9f9;
            padding: 30px;
            height: 300px;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        .product-card img {
            max-width: 100%;
            max-height: 100%;
            object-fit: contain;
            transition: var(--transition);
        }

        .product-card:hover img {
            transform: scale(1.1);
        }

        .product-details {
            padding: 20px;
        }

        .p-name {
            font-weight: 700;
            font-size: 16px;
            margin-bottom: 8px;
            height: 44px;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .p-price {
            font-size: 18px;
            font-weight: 800;
            color: var(--primary-dark);
        }

        .btn-group-custom {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 10px;
            margin-top: 15px;
        }

        .btn-view {
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 12px;
            text-align: center;
            text-decoration: none;
            color: #000;
            font-weight: 600;
            font-size: 14px;
            transition: var(--transition);
        }

        .btn-view:hover {
            background: #000;
            color: #fff;
        }

        .btn-add-cart {
            background: var(--accent-color);
            color: white;
            border: none;
            padding: 10px;
            border-radius: 12px;
            font-weight: 700;
            font-size: 14px;
            transition: var(--transition);
        }

        .btn-add-cart:hover {
            background: #ab8b4a;
            box-shadow: 0 5px 15px rgba(197, 160, 89, 0.3);
        }

        /* ===== CART OFFCANVAS ===== */
        .cart-canvas { border-radius: 30px 0 0 30px; }
        .cart-item-new {
            display: flex;
            gap: 15px;
            padding: 15px;
            background: #fff;
            border-radius: 15px;
            margin-bottom: 15px;
            border: 1px solid #eee;
        }

        /* ===== FOOTER ===== */
        footer {
            background: var(--primary-dark);
            color: #fff;
            padding: 80px 5% 30px;
        }

        .footer-grid {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 1fr;
            gap: 50px;
        }

        @media (max-width: 992px) {
            .footer-grid { grid-template-columns: 1fr 1fr; }
            header { padding: 15px 20px; }
        }
    </style>
</head>

<body>

    <header>
        <div class="logo">Heet <span>.</span></div>

        <div class="search-container d-none d-md-block">
            <input type="text" id="searchInput" placeholder="Search our collection...">
        </div>

        <div class="d-flex align-items-center gap-4">
            <a href="#" class="text-dark text-decoration-none fw-600 d-none d-lg-block">Login</a>
            <button class="btn position-relative" data-bs-toggle="offcanvas" data-bs-target="#cartCanvas" style="font-size: 24px;">
                👜
                <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-dark" style="font-size: 10px;">
                    <%= cartCount %>
                </span>
            </button>
        </div>
    </header>

    <div class="nav-categories">
        <% category.forEach(cat => { %>
        <div class="category-item dropdown">
            <%= cat.categoryName %>
        </div>
        <% }) %>
    </div>

    <main>
        <h2 class="section-title">New Arrivals</h2>
        
        <div class="products-grid">
            <% product.forEach(p => { %>
            <div class="product-card">
                <div class="image-wrapper">
                    <img src="<%= p.productImage %>" alt="<%= p.productName %>">
                </div>
                <div class="product-details">
                    <div class="p-name product-name"><%= p.productName %></div>
                    <div class="p-price">₹<%= p.productPrice %></div>
                    
                    <div class="btn-group-custom">
                        <a href="/web/singleview/<%= p.id %>" class="btn-view">Details</a>
                        <form action="/web/addtocart/<%= p._id %>" method="post">
                            <button type="submit" class="btn-add-cart w-100">Add +</button>
                        </form>
                    </div>
                </div>
            </div>
            <% }) %>
        </div>
    </main>

    <div class="offcanvas offcanvas-end cart-canvas" tabindex="-1" id="cartCanvas">
        <div class="offcanvas-header border-bottom">
            <h5 class="fw-bold m-0">Your Bag</h5>
            <button type="button" class="btn-close" data-bs-dismiss="offcanvas"></button>
        </div>
        <div class="offcanvas-body bg-light">
            <% if(cartItems.length === 0){ %>
                <div class="text-center py-5">
                    <p class="text-muted">Your bag is empty.</p>
                </div>
            <% } %>

            <% cartItems.forEach(item => { %>
                <div class="cart-item-new shadow-sm">
                    <img src="<%= item.productId.productImage %>" style="width: 60px; height: 60px; object-fit: contain;">
                    <div class="flex-grow-1">
                        <div class="fw-bold small"><%= item.productId.productName %></div>
                        <div class="text-muted small">₹<%= item.productId.productPrice %></div>
                        <div class="d-flex align-items-center justify-content-between mt-2">
                            <input type="number" class="form-control form-control-sm qty-input" 
                                   value="<%= item.quantity %>" style="width: 60px" 
                                   data-price="<%= item.productId.productPrice %>">
                            <a href="/web/deletecart/<%= item._id %>" class="text-danger small text-decoration-none">Remove</a>
                        </div>
                    </div>
                </div>
            <% }) %>

            <div class="mt-4 p-3 bg-white rounded-3 shadow-sm">
                <div class="d-flex justify-content-between mb-3">
                    <span class="fw-bold">Grand Total</span>
                    <span class="fw-bold text-success">₹<span id="grandTotal">0</span></span>
                </div>
                <button class="btn btn-dark w-100 py-3 fw-bold rounded-pill">Checkout Now</button>
            </div>
        </div>
    </div>

    <footer>
        <div class="footer-grid">
            <div>
                <h3 class="fw-800">Heet .</h3>
                <p class="text-muted mt-3">Curating the finest products for the modern lifestyle. Quality and elegance in every click.</p>
            </div>
            <div>
                <h6 class="fw-bold mb-4">Shop</h6>
                <ul class="list-unstyled text-muted">
                    <li>All Products</li>
                    <li>Featured</li>
                    <li>Discount</li>
                </ul>
            </div>
            <div>
                <h6 class="fw-bold mb-4">Support</h6>
                <ul class="list-unstyled text-muted">
                    <li>Shipping</li>
                    <li>Returns</li>
                    <li>Contact</li>
                </ul>
            </div>
            <div>
                <h6 class="fw-bold mb-4">Follow Us</h6>
                <div class="d-flex gap-3">
                    <span>IG</span> <span>FB</span> <span>TW</span>
                </div>
            </div>
        </div>
        <div class="text-center mt-5 pt-4 border-top border-secondary text-muted small">
            © 2026 Heet Store. Built for Elegance.
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        // Smooth Total Updates
        function updateTotals() {
            let grandTotal = 0;
            document.querySelectorAll('.qty-input').forEach(input => {
                let qty = parseInt(input.value) || 1;
                let price = parseInt(input.dataset.price);
                grandTotal += (qty * price);
            });
            document.getElementById('grandTotal').innerText = grandTotal.toLocaleString();
        }

        document.querySelectorAll('.qty-input').forEach(input => {
            input.addEventListener('change', updateTotals);
        });

        // Instant Search
        document.getElementById("searchInput").addEventListener("keyup", function() {
            let filter = this.value.toLowerCase();
            document.querySelectorAll(".product-card").forEach(card => {
                let title = card.querySelector(".product-name").innerText.toLowerCase();
                card.style.display = title.includes(filter) ? "block" : "none";
            });
        });

        updateTotals();
    </script>
</body>
</html>